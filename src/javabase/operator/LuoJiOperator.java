package javabase.operator;

/**
 * 逻辑操作符
 */
public class LuoJiOperator {
    // & 长路与  && 短路与    两边的运算单元都是布尔值  都为真时，才为真  任意为假，就为假
    // 区别
    //  长路与 两侧，都会被运算
    //  短路与 只要第一个是false，第二个就不进行运算了

    // | 长路或  || 短路或
    // 区别
    //  长路或 两侧，都会被运算
    //  短路或 只要第一个是false，第二个就不进行运算了

    // 取反 !

    // 异或 ^  不同，返回真   相同，返回假
}
